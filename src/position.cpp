#include "position.h"

Position::Position(int row, int column)
{
    this->row = row; //asigna el valor a la variable de la clase
    this->column = column; //asigna el valor a la variable de la clase
}